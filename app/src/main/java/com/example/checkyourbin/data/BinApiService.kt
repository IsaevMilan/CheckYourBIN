package com.example.checkyourbin.data

import com.google.firebase.crashlytics.buildtools.reloc.org.apache.http.client.HttpClient

/*
interface BinApiService {
    suspend fun getBinInfo(bin: String): BinInfo
}

class BinApiServiceImpl : BinApiService {
    private val client = HttpClient()

    override suspend fun getBinInfo(bin: String): BinInfo {
        return client.get("https://lookup.binlist.net/$bin")
    }
}*/
